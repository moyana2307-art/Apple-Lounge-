# Railway deployment config — place at backend/
buildCommand = "npm install"
startCommand = "node server.js"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
